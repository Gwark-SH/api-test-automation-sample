# 🚀 API Test Automation Portfolio

## 👋 소개
안녕하세요! **"데이터 정합성"과 "테스트 안정성"을 최우선으로 생각하는 1년 차 QA 엔지니어**입니다.
이 저장소는 제가 실무에서 Postman을 활용해 API 테스트를 자동화했던 로직 중, 핵심이 되는 검증 시나리오를 정리한 샘플입니다.

## 📂 주요 코드 설명

### 1. 데이터 교차 검증 (Order_Validation_Scenario.js)
- **목적:** 주문 생성 API 호출 시, 요청(Request) 데이터가 DB에 정확히 반영되었는지 응답(Response)과 비교하여 검증합니다.
- **특징:**
  - 단순히 `200 OK`만 확인하는 것이 아니라, **가격/수량/마켓 타입** 등 비즈니스 중요 데이터를 전수 검사합니다.
  - JSON Schema의 필수 키(Key) 누락 여부를 체크하여 방어 로직을 구축했습니다.

### 2. 비동기 재시도 로직 (Async_Retry_Logic.js)
- **목적:** 주문 체결이나 외부 연동처럼 처리가 지연되는 API를 테스트하기 위한 **Polling(재시도) 메커니즘**입니다.
- **특징:**
  - `setTimeout`과 `postman.setNextRequest`를 활용해 원하는 상태값(`5600`)이 나올 때까지 자동으로 대기하고 재요청합니다.
  - 네트워크 지연(Latency)이나 서버 부하 상황에서도 테스트가 실패(Fail)하지 않도록 안정성을 높였습니다.

## 🛠️ 활용 도구
- **Tools:** Postman (JavaScript Sandbox)
- **Key Concepts:** JSON Parsing, Assertion, Asynchronous Testing, Data Integrity
